# 常用块回归稿

这是一段普通正文，用来对照预览和公众号里的段落间距、字号、行距。

## 二级标题：小节

### 三级标题：更小一层

下面是一段引用。贴进去之后，左侧竖线或底色应还在。

> 打开就要看见成品。贴进去和看见的一样。

无序列表：

- 第一项：只看层级，不看装饰
- 第二项：换主题后整表应跟着变色
- 第三项：手机预览里不要串行

有序列表：

1. 先看成稿
2. 再复制
3. 最后贴进后台

代码块（对照字体和背景有没有被洗掉）：

```js
function greet(name) {
  return `hello, ${name}`
}
```

表格：

| 列一 | 列二 | 列三 |
| --- | --- | --- |
| 预览 | 后台 | 手机 |
| 有线 | 有线 | 能读 |

---

外链应在复制后变成文末引用（先打开「全局样式 → 微信外链转底部引用」）：请对照 [墨笔仓库](https://github.com/FengBuL/mobi) 是否出现在文末，正文里是否还留着可点的原文。
