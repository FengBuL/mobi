# 已转存 mmbiz 图（可选格）

把下面这行里的地址换成你已经在公众号素材库里的图。域名必须是 `mmbiz.qpic.cn` 或 `mmbiz.qlogo.cn`。没有现成地址就跳过本格，结果表填「未测」。

![已转存 mmbiz 图](https://mmbiz.qpic.cn/REPLACE_WITH_YOUR_MMBIZ_URL)

对照：复制按钮旁**不应**再为这一张报「会丢」。后台和手机预览都应直接看见，地址仍是 mmbiz。
