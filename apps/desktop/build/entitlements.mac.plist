<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Chromium 要 JIT，所以 allow-jit 和 unsigned-executable-memory 是 Electron 应用的标配。

  disable-library-validation 是 ad-hoc 签名特有的：hardened runtime 默认只允许加载
  同一开发者签名的库，而 ad-hoc 签名没有开发者身份，Electron Framework 会被判定成
  «别人的库» 而拒绝加载，应用起不来。等以后有了 Developer ID 证书，这一条可以去掉。
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
  </dict>
</plist>
