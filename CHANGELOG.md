# 更新日志

本文件记录对外可见的产品变更。未发布的条目只准备，不打 tag、不推送。已发布版本以 GitHub Release 为准。

格式对照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 SemVer。粘贴与图床相关的句子都能对到 TASK-06 记录表，不写「贴进去 100% 一样」。

## [未发布]

### 修复

- 连续更换方案或版式时不再卡死，预览落到最后一次选择；换版式退出方案时不会被下拉回传还原回去
- 连着换好几个方案后不再出现「下拉换了、预览不动」：记录撤销点时的内部错误会吞掉整次换方案，现在撤销点记不下来也照样把方案换过去
- 正在应用样式的这一瞬间再点方案或版式，选择会排到后面执行，不再被丢掉

### 变更

- 打开封面改为「为编辑而生」
- 点预览里的标题、引用或列表会保持选中并打开组件库，样式面板切到「当前组件」，不再被「全局样式」抢走
- 主题条栏够宽时露出超过 5 套，窄时仍是 5 套
- 「当前组件」和图片组件库不再跟着正文主题字体变，和「全局样式」用同一套界面字体

## [2.3.0] - 2026-08-19

第一屏重写、存稿文件化、图床叙事按真机事实改写。相对公开 `v2.2.4` 是 minor，不是 patch。

### 新增

- 打开即简洁：不弹工作区问卷；默认稿是一篇短公众号，语法课进帮助；新稿标题「未命名」
- 预览上方换主题：第一层专栏 / 科技 / 教程 / 克制 / 中式，其余进「更多」；「更多」里用默认稿渲成成稿小样，不再只是文字列表
- 点选换板块：没选中先提示；选中只出对应类别；已单独处理的块换主题后有说明，并可还原为普通文本
- 插图一个入口；图床对外只强调公众号素材库
- 预览按贴完结果打标：长图会变成公众号长图、固定裁剪会按原图比例铺满、压图角标和渐变标题条会落到图下；并写出路。配了公众号图床时不再打裁切标（复制会先裁再上传）。长图可「切成多张」；公众号图床 IP 不在白名单时切片仍留在稿里（本机图），只提示一次把该 IP 加进白名单，不再弹出微信原文
- GitHub Issues 按主路径分三类：贴进去不一样 / 图丢了 / 主题或板块问题，都要求主题名、预览与后台截图、是否配图床
- 主题 SVG / 纸纹在预览和板块库缩略图里不画，避免以为贴得进
- 存稿可落成本地 Markdown 文件（桌面版、Chrome / Edge 网页版）。打开的文件夹是导入源和整理盘：点文件导入到编辑器，改稿和版式不写回原文件。可勾选多篇一起归档或移动；归档里可取消归档。有稿的子文件夹不能直接删。Safari / Firefox 仍在浏览器里，约每 14 天提醒导出 Markdown
- 发版前必跑六格回归：A1、B1、B2、C5、C6、C7，见 `docs/wechat-paste-regression/README.md`
- 我的号：一份本地配置记住主题、字体、字号、主题色、图床引用和外链引用开关。每个号有自己的当前稿，切号不会改到另一号的正文。单号界面不变；建第二个号后主题条和设置里出现切换。删除号不删稿，孤儿稿落回默认号。每个号记住最近用过的板块样式。可导出/导入号配置以便换电脑，文件含图床密钥、不含稿正文。导入同名时可覆盖或作为新号。无账号无云

### 变更

- 默认稿首屏不再同步加载 MathJax。稿里出现公式才拉 tex-svg.js。PlantUML / AntV / Mermaid 本来就是用到再加载
- 未转存图仍能复制。按钮旁写「还有 N 张不是公众号地址，微信可能留下或丢掉」，不再说「都会丢」
- 不配图床直接贴是合法主路径。微信不会可靠转存外链（TASK-06 事实 A）
- 公众号素材库写清资质门槛：认证主体 + API IP 白名单；40164 不是密钥填错
- 官方代理 `api.mobieditor.cn` 未 `/health` 成功前，不再写成生产默认
- 对外文案去掉「同步」「KaTeX」「贴进去不走样」等与实现不符的词
- 简洁模式样式面板只留字体 / 字号 / 主题色
- 匿名使用统计默认关闭，开关从「关于」移到「设置」
- 发版前六格 A1 / B1 / B2 / C5 / C6 / C7 于 2026-08-18 真机通过（用户确认）

### 安全

- `/fetch-image` 内网与元数据黑名单、重定向后二次校验、收紧 `ALLOWED_ORIGINS`
- 图床界面写明 AppSecret 存在本机、有被读走的风险
- 自定义图床 `new Function` 默认关闭，需显式确认；确认页写明会在本机执行任意脚本

### 已知取舍（不是回归）

- 微信不会可靠地把外链转成 mmbiz。Wikimedia 常插入失败；Unsplash / picsum / httpbin / 本机图后台有时还在
- `scroll-window` 复制产物仍带 `overflow` / `transform`，粘贴安全脚本会标红，对应已知长图降级
- 中式 / 科技第一眼卖相会弱一些：预览不再画贴不进的纹路
- 桌面安装包只出 macOS 和 Windows。Linux 请用网页版

## [2.2.4] - 2026-08-15

公开基线。见 GitHub Release。
